1. Line 9 prints the following:
values added:  20

2. Line 13 prints the following: 
final result:  20

3. Line 9 prints the follwoing: 
values added: 20

4. Returns error because the it is not in scope originally declared 

5. Return error because we are tyring to modify a const. Const is fixed and cannot be changed once declared. 

6. Return error because const is not within scope outside of the function declared 
